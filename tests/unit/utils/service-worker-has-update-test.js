import serviceWorkerHasUpdate from 'dummy/utils/service-worker-has-update';
import { module, test } from 'qunit';

module('Unit | Utility | service worker has update');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = serviceWorkerHasUpdate();
  assert.ok(result);
});
